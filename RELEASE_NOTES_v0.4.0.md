## TEGG Touch 蛋挞 v0.4.0

赛车场景重做、运行时悬浮球重做、内置一键自动升级。一个大版本三件大事。

--------

### 🏎️ 方向盘「轻松操控」模式（新建方向盘默认就是它）

赛车游戏现在可以纯鼠标玩了，零配置：

- **鼠标横向移动 → A / D 转向**（键盘），停下来 80ms 后自动释放 → 方向盘以 720°/sec 回正
- **鼠标上下移动 → 手柄 RT 油门**（连续累加，向上加、向下减）
- **鼠标指定键按下 → S 刹车**（键盘），默认左键，可改成右 / 中 / 侧 1 / 侧 2
- **整个屏幕都是触发区域**，不用瞄准方向盘
- **视觉动画**：360°/sec 锁入、720°/sec 回正，EMA 平滑过滤手腕抖动，回中干净不残角
- **方向盘 HUD 指示器**：可显隐，实时显示当前转向 / RT / 刹车

复杂模式（原 v0.3.x 整套方向盘）保留在编辑器另一个 tab 里，老用户无损。

### 🎛️ 方向盘编辑器重做：1280×960 侧栏布局

- 左侧栏 260：[轻松操控] [复杂设置] tab，点切换即激活，行内右侧带橙色「激活」徽章
- 右侧 960 内容区，底部按钮跟内容对齐（不通栏）
- 复杂模式参数布局更紧凑：响应有效区域 / 中心死区 / 视觉旋转角度（5 档吸附滑块 90°/180°/270°/360°/720°）
- 「其他鼠标按键配置」按钮在轻松模式自动隐藏

### 🫧 悬浮球（F4 / 工具栏「悬浮球」按钮）

F4 不再把整条工具栏折掉，而是召唤一个 56×56 的蛋挞悬浮球：

- **左键**：切换工具栏显隐
- **右键**：切换按键显隐（等同 F7）
- **中键**：停止运行，回到编辑模式
- **拖动**（>4px）：移动悬浮球
- **位置写入 profile config**，跨重启都记得
- 工具栏「悬浮球」按钮激活时变琥珀色

### 🔄 一键自动升级（全新）

不再让用户去 GH 手动下载 + 解压：

- UpdateDialog 改「立即更新」+ 进度条
- 全量 zip 后台下载到 `%TEMP%` → 写 PowerShell 升级器 → 主程序退出 → 升级器接管解压覆盖 → 保留 `profiles/` `settings/` `logs/` `config.json` → 自动重启
- **升级中弹深色 splash 窗口**（520×200，无边框，TopMost），状态实时滚动（等待退出 → 解压 → 替换 → 启动），不让用户以为崩了
- **多镜像 fallback**：本地 OSS（大陆加速）→ GH 直连 → ghproxy / gh-proxy / mirror.ghproxy
- 下载前自动清 `%TEMP%` 旧 zip，不会因为多次失败累积撑爆 C 盘

### 🎨 其他 UI 改进

- **回中带双击弹窗**视觉对齐按钮编辑器（18px 标题 + 图标关闭 + 提示直显不再被卡片框）
- **设置弹窗**「悬浮球」描述加 word-wrap，长文案不再被截断

### 🔓 开源安全

- 自动升级用的私有 OSS bucket / endpoint 拆到 `.gitignore` 排除的本地配置文件
- 任何 fork 蛋挞的人都不会看到我们的镜像私有信息；想加自家 CDN 仿照创建俩文件即可

--------

### 📥 下载

`TEGGTouch_v0.4.0.zip` 解压即用，无需安装。

⚠️ **手柄模式提示**：首次进入手柄模式 / 用方向盘 RT 油门会提示装 ViGEmBus 驱动，跟着弹窗一步步操作即可。轻松模式的 A/D/S 走键盘事件，没装驱动也能用；RT 油门需要手柄通道。

⚠️ **从 v0.3.2 升级**：当时还没有自动升级机制，需要**手动**下载 v0.4.0 zip 解压覆盖（保留 `profiles/`、`settings/`、`logs/`、`config.json`）。从 v0.4.0 开始，下个版本就能在 app 内一键升级了。

### 💡 谁可以使用蛋挞？

🦾 单手用户 — 只要能操作鼠标，就能玩需要键鼠配合的游戏
🎮 手部不便的玩家 — 把复杂的多键操作简化为鼠标移动和点击
♿ 需要无障碍支持的用户 — PC 游戏全新无障碍交互方式
🏎️ 想用鼠标玩手柄游戏的玩家 — 现在赛车 / 动作 / 平台游戏也能纯鼠标
