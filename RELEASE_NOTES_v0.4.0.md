## TEGG Touch 蛋挞 v0.4.0

赛车场景重做、运行时悬浮球重做、内置一键自动升级。一个大版本三件大事。

--------

### 🏎️ 方向盘「轻松操控」模式（新建方向盘默认就是它）

赛车游戏现在可以纯鼠标玩了，零配置：

- **鼠标横向移动 → A / D 转向**，停下方向盘自动回正
- **鼠标上下移动 → 油门**（向上加、向下减，连续累加）
- **鼠标指定键按下 → 刹车**，默认左键，可改成右 / 中 / 侧 1 / 侧 2
- **整个屏幕都是触发区域**，不用瞄准方向盘
- 方向盘动画顺滑，打方向匀速、松手匀速回正、平滑过中位
- 屏幕上的方向盘 HUD 实时显示当前转向 / 油门 / 刹车状态，可显隐

老的复杂模式（手动调摇杆 / LT / RT）保留在编辑器另一个 tab 里，老用户无损。

### 🎛️ 方向盘编辑器重做

打开后干净清爽：左侧栏选「轻松操控」或「复杂设置」，右侧 960 宽度展示当前模式的全部参数。选中的 tab 上带「激活」橙色徽章。

复杂模式的参数也重新排了：响应有效区域 / 中心死区 / 视觉旋转角度（90° / 180° / 270° / 360° / 720° 五档可选），布局更紧凑。

### 🫧 悬浮球（F4 / 工具栏「悬浮球」按钮）

F4 不再把整条工具栏折叠掉。按 F4 召唤一个 56×56 的蛋挞悬浮球：

- **左键**：切换工具栏显隐
- **右键**：切换按键显隐（等同 F7）
- **中键**：停止运行，回到编辑模式
- **拖动**：把球挪到顺手的位置；下次打开记得在哪
- 工具栏「悬浮球」按钮在球弹出时变琥珀色，状态一目了然

### 🔄 一键自动升级（全新）

不用再跑去 GitHub 手动下载、解压、迁移配置：

- 升级提示弹窗里直接「立即更新」
- 后台下载，进度条实时显示
- 主程序关闭后弹出深色升级窗口，状态滚动提示（解压中 / 替换文件中 / 启动新版本中）
- 升级期间你的所有 profile、设置、日志原封不动
- 完成后新版本自动起来
- 支持大陆下载加速，国外慢/挂的时候自动切走

### 🎨 其他小改进

- 回中带双击弹出的小窗口样式跟主编辑器对齐了，更协调
- 设置弹窗里悬浮球功能的说明文字加了自动换行，长句不再被截断

--------

### 📥 下载

下载 `TEGGTouch_v0.4.0.zip` 解压即用，无需安装。

⚠️ **手柄模式提示**：首次切到手柄模式 / 使用方向盘的 RT 油门时会提示安装 ViGEmBus 驱动，跟着弹窗一步步操作即可。轻松操控的 A/D/S 是键盘事件，不装驱动也能用；RT 油门要走手柄通道。

⚠️ **从 v0.3.x 升级**：当时还没有自动升级机制，需要**手动**下载 v0.4.0 zip 解压覆盖（保留 `profiles/`、`settings/`、`logs/`、`config.json`）。从 v0.4.0 开始，下个版本就能在 app 内一键升级了。

### 💡 谁可以使用蛋挞？

🦾 单手用户 — 只要能操作鼠标，就能玩需要键鼠配合的游戏
🎮 手部不便的玩家 — 把复杂的多键操作简化为鼠标移动和点击
♿ 需要无障碍支持的用户 — PC 游戏全新无障碍交互方式
🏎️ 想用鼠标玩手柄游戏的玩家 — 现在赛车 / 动作 / 平台游戏也能纯鼠标
