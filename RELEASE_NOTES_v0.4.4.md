## TEGG Touch 蛋挞 v0.4.4

一个**功能大更新**：候选键位全面打通、摇杆新增 WASD 模式、可用语音/按键**启动本地应用**、新增**回中模式**，语音设置也重做了一遍。

> 💡 **建议所有用户升级** —— 这一版把「任意按钮映射任意操作」彻底打通，还能让语音直接开游戏、一句话把光标拉回准星。

--------

### 🎹 候选键位统一化

所有编辑器（按钮 / 摇杆 / 方向盘 / 语音 / 宏）右侧的候选键位统一成全套，**任意按钮都能映射任意操作**：

- 固定四类候选 Tab，顺序统一为 **键盘 / 手柄 / 鼠标 / 自定义宏**
- 宏池统一：键盘宏和手柄宏合并成一个池，旧配置自动迁移
- 宏编辑器的候选项也改成同款 Tab，和单个配置完全等价
- 手柄按钮候选在驱动未就绪时显示安装引导

### 🕹️ 摇杆新增 WASD 模式

摇杆「摇杆ID」除左 / 右摇杆外新增 **WASD 模式**：把圆盘按 8 个方向映射成方向键（上下左右单键、斜向同时两键），**死区 / 释放阈值照常生效**，4 个方向键位可自定义（默认 WASD），是否吸附八方向可选。摇杆图标用方向键簇造型区分。

### 🚀 语音 / 按键启动本地应用

新增「应用」候选 Tab：自动扫描本机已安装的程序（带图标、可搜索 / 刷新），**配置后用语音或按键就能一键打开对应程序**（游戏、软件都行）。

### 🎯 回中模式

语音指令和回中带按钮新增「回中」能力——一句话 / 一个触发把光标拉回到目标中心：

- **屏幕中心** / **中心环** / **方向盘** / **指定摇杆** 的中心
- 回中目标按当前布局动态列出；语音指令用「行为卡片」在「触发按键」和「触发回中」之间切换
- 摇杆现在会自动命名（未命名01/02…），方便识别和选择

### 🎙️ 语音设置面板重做

- 「运行时启用语音」开关移到麦克风状态行、更紧凑
- 测试指令 / 保存等按钮统一配色，指令列表改为圆角卡片样式
- 修复：语音 click 时序问题（识别成功却没输出到游戏）已解决

--------

### 📥 下载 & 升级

- **v0.4.x 用户**：在 app 里点「立即更新」即可一键升级，所有 profile、设置、日志都会保留（国内走 OSS 镜像加速）
- **首次安装**：下载 `TEGGTouch_v0.4.4.zip` 解压即用，无需安装 Python

### 💡 谁可以使用蛋挞？

🦾 单手用户 — 只要能操作鼠标，就能玩需要键鼠配合的游戏
🎮 手部不便的玩家 — 把复杂的多键操作简化为鼠标移动和点击
♿ 需要无障碍支持的用户 — PC 游戏全新无障碍交互方式
🏎️ 想用鼠标玩手柄游戏的玩家 — 赛车 / 动作 / 平台游戏也能纯鼠标
