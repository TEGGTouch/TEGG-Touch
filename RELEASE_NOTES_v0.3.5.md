## TEGG Touch 蛋挞 v0.3.5 (测试版)

> 这个版本主要是**试跑自动升级流程** — 加了「立即更新」按钮，能自动下载新版 zip、覆盖到原目录、保留用户数据、删掉下载的压缩包。后续会用 v0.3.6 验证升级链路打通后再发正式 v0.3.5。

下面的功能改动跟正式版 v0.3.5 一致：

赛车场景大改：方向盘新增「轻松操控模式」，把整个屏幕变成方向盘 — 鼠标横向移动 = A/D 转向、纵向 = RT 油门、左键 = S 刹车。新建的方向盘默认就用这套，零配置即可玩赛车。运行时悬浮球重做，工具栏不再被「收起」，球本身变成显隐 / 停止的快捷面板。方向盘编辑器迁到左侧栏布局，模式切换 + 激活 tag 更清晰。

--------

### 🏎️ 方向盘「轻松操控」模式（默认）

赛车游戏可以纯鼠标玩了：

- **鼠标横向位移 → A / D 键盘转向**：往左推 = A，往右推 = D；停下 80ms 后自动释放，方向盘以 720°/sec 回正
- **鼠标纵向位移 → 手柄 RT 油门**：向上累加、向下减少；累计值持续输出给手柄
- **鼠标左键 → S 键盘刹车**：按下 = S，松开释放
- **整个屏幕都是触发区域**：不需要鼠标落在方向盘内
- **方向盘视觉指示器**：仍然作为 HUD 显示当前转向 + RT + 刹车状态
- **平滑动画**：转向以 360°/sec 滑向 ±180°，松开以 720°/sec 回正；EMA 平滑过滤手抖，回中干净不留残角
- **新建方向盘默认就是轻松模式**，复杂模式需要的玩家可手动切「复杂设置」tab

### 🎛️ 方向盘编辑器重做：侧栏布局

匹配设置弹窗的视觉风格，1280×960：

- 左侧栏：[轻松操控] [复杂设置] 两个 tab，**点切换即激活**，选中项右侧带橙色「激活」圆角徽章
- 右侧 960×960 内容区：当前模式的所有配置
- 底部按钮跟内容对齐，不再通栏
- 复杂模式专属的「其他鼠标按键配置」按钮在轻松模式下自动隐藏
- 复杂模式：「响应有效区域」(原「释放阈值」)、中心死区、视觉旋转角度 (90/180/270/360/720 五档滑块) 等参数布局更紧凑

### 🫧 运行时悬浮球重做（F4 / 收起按钮 → 悬浮球）

F4 不再把工具栏折成方块，而是召唤一个 56×56 的「蛋挞」悬浮球，工具栏保持不动：

- **F4 / 工具栏「悬浮球」按钮**：切换悬浮球显隐；激活时按钮变琥珀色
- **悬浮球左键**：切换工具栏显隐
- **悬浮球右键**：切换按键显隐（等同 F7）
- **悬浮球中键**：停止运行，返回编辑模式
- **拖动**：移动悬浮球；位置写入当前 profile 配置，跨会话/重启都记得
- **首次召唤**：出现在工具栏左侧 20px 处

### 🎨 其他 UI 改进

- **回中带双击弹窗**：对齐按钮编辑器风格 (18 px 标题左对齐 + 图标关闭按钮)，提示字号加大且不再被卡片框住，文字直显
- **方向盘视觉旋转角度**：改为 5 档吸附滑块 (90 / 180 / 270 / 360 / 720)，180° 默认有蓝色三角标记
- **方向盘参数布局**：「响应有效区域」独占一行，「中心死区 + 视觉旋转角度」并排；「灵敏度曲线」独占一行
- **快捷键设置弹窗**：长说明文字加 word-wrap，不会被截断

--------

### 📥 下载

下载 `TEGGTouch_v0.3.5.zip` 解压即用，无需安装。

⚠️ **手柄模式提示**：首次切到手柄模式 / 使用方向盘的 RT 油门时会提示安装 ViGEmBus 驱动，跟着弹窗一步步操作即可。轻松操控的 A/D/S 是键盘事件，没装驱动也能用，但 RT 油门要走手柄通道。

### 💡 谁可以使用蛋挞？

🦾 单手用户 — 只要能操作鼠标，就能玩需要键鼠配合的游戏
🎮 手部不便的玩家 — 将复杂的多键操作简化为鼠标移动和点击
♿ 需要无障碍支持的用户 — PC 游戏全新无障碍交互方式
🏎️ 想用鼠标玩手柄游戏的玩家 — 现在赛车 / 动作 / 平台游戏也能纯鼠标
