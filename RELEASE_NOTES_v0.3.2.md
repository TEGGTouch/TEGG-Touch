## TEGG Touch 蛋挞 v0.3.2

v0.3.1 之后的稳定性小版本：修复一个会导致整个 app 崩溃的严重 bug，重构手柄驱动检测以避免误判与误导，统一所有「驱动有问题」类弹窗的友好文案，诊断包改最近 10 条 + 固定文件名便于发给开发者。

--------

### 🐛 严重 bug 修复

- **点击空白区域 → 整个 app 直接闪退**（v0.3.1 / v0.3.0 / 旧 v0.2.x 都中）
  - 根因：`mouse_event` 的 `dwExtraInfo` 参数（Win32 是 `ULONG_PTR` 整数）旧 ctypes 声明为 `POINTER(c_ulong)`，Python 3.14 起严格类型校验，传 int 0 立即抛 `ctypes.ArgumentError`
  - 修复：声明改成 `c_size_t`（平台无关的指针大小整数，跟 Win32 ABI 对齐），调用代码不变
  - 触发场景：任何模式下点击空白区域 → 智能穿透转发点击到底层窗口 → 崩

### 🎮 手柄驱动检测重构

之前的「检测 vgamepad Python 库」函数顺带触发了 `VBus()` 内核连接，导致**驱动有问题时被误判为「Python 库未装」**。用户被引导去 pip 安装（pip 装好也没用，因为根因是驱动），走到 fallback 才知道要重装驱动。

- **`_vgamepad_lib_available` 改用 `importlib.util.find_spec`**：只检测包文件存在，不触发驱动连接；驱动问题正确归类为 `DRIVER_BROKEN` 走"重新安装"流程
- **PyInstaller 打包模式 (`sys.frozen`) 强制 lib_ok=True**：打包用户的 vgamepad 永远嵌在 `_internal/vgamepad/`，且 pip 在 .exe 进程里跑不起来 — 从源头避开整条 LIB_MISSING 路径

### 💬 安装弹窗文案统一

不出现「损坏」「需要重启电脑」「驱动状态异常」等吓人技术词。「装了但有问题」的几个状态都用同一个友好动作：

- `DRIVER_BROKEN`：「🔄 手柄驱动需要重新安装」+ 按钮「重新安装」
- `NEEDS_REBOOT`：同上文案 + 按钮「重新安装 / 关闭」（不再只能"知道了"被动退出，多数情况下再装一次就能用，不必让用户去重启电脑）
- 双语同步（zh-CN / en）

### 📋 诊断包导出改进

- 最近 **5 → 10** 条会话日志（跟 `KEEP_SESSIONS=10` 对齐，本地保留多少就全打包）
- 文件名固定为 **`蛋挞debug日志.zip`**（之前带时间戳，桌面容易堆积）
  - 重复导出**直接覆盖**桌面那一份，始终只有一份最新的，发给开发者免找
  - 不足 10 条有多少打多少（list_recent_log_paths 已是 `paths[:n]` 语义，无需特殊处理）

--------

### 📥 下载

下载 `TEGGTouch_v0.3.2.zip` 解压即用，无需安装。

⚠️ **手柄模式提示**：首次切到手柄模式时会提示安装 ViGEmBus 驱动，跟着弹窗一步步操作即可。如果不用手柄模式只用键盘模式，可以忽略 ViGEmBus，蛋挞会正常启动。

### 💡 谁可以使用蛋挞？

🦾 单手用户 — 只要能操作鼠标，就能玩需要键鼠配合的游戏
🎮 手部不便的玩家 — 将复杂的多键操作简化为鼠标移动和点击
♿ 需要无障碍支持的用户 — PC 游戏全新无障碍交互方式
🏎️ 想用鼠标玩手柄游戏的玩家 — 现在赛车 / 动作 / 平台游戏也能纯鼠标
