## TEGG Touch 蛋挞 v0.3.1

TEGG Touch 是一款专为 PC 环境设计的无障碍辅助软件，永久免费、完全开源。仅用鼠标简单的移动和点击，就能替代大部分游戏操作，为单手用户和手部不便的玩家提供无障碍支持。

本次是自 v0.2.1 以来的**重大版本升级**，新增完整的手柄模拟模式，方案管理和中心轮盘全面重构，方向盘扳机加了重磅新模式「浮标点击」，其他鼠标按键完整配置 + 中心 hub 视觉反馈 + 中心死区等改进，许多 UX 细节打磨，并修复了若干会影响使用的 bug（**包括「未装 ViGEmBus 驱动时启动崩溃」**、更新弹窗顶不到最上层、手柄按键 click 偶尔触发不到、双击多次会开多个编辑器等）。

--------

### 🎮 手柄模拟模式（重磅新增）

蛋挞现在能模拟 Xbox 360 手柄！基于 ViGEmBus 虚拟手柄驱动，让需要手柄的游戏（赛车、动作、独立游戏等）也能用鼠标玩。

- **一键安装驱动**：首次切到手柄模式自动检测 ViGEmBus 状态（5 种状态：已就绪/需安装/需更新/损坏/需重启），离线/在线/手动三级安装弹窗指引完成
- **未装驱动不影响蛋挞启动**：键盘模式完全可用，切到手柄模式时才弹安装引导
- **工具栏新增手柄模式**：键盘/手柄下拉切换，每个方案独立记忆模式偏好
- **三种手柄控件**：
  - 🔘 **手柄键 (gp_button)** — 单键映射，A/B/X/Y/LB/RB/十字键等
  - 🕹️ **摇杆 (gp_stick)** — 圆形交互区，鼠标位置映射摇杆方向，可配死区/灵敏度曲线/八方向锁定，并支持鼠标其他键作为额外触发源
  - 🏎️ **方向盘 (gp_wheel)** — 方形交互区 + 两侧浮挂 LT/RT 视觉进度条，鼠标横向控制转向，LT/RT 可选 4 种控制方式（浮标点击/滚轮/垂直位移/左右键）互斥
- **手柄宏 per-profile**：跟键盘宏一样独立可配，前缀 `gpmacro:`

### 🏎️ 方向盘扳机第 4 种模式「浮标点击」（默认）

最适合赛车 / 弓箭蓄力 / 任何「精准给值」场景：

- 鼠标上下移动 → 方向盘 LT/RT 进度条上显示一根**横线浮标**（LT 玫红 / RT 琥珀黄）
- **浮标移动不直接修改扳机值** — 这跟「垂直位移」模式不同
- 按下你配的鼠标键（左键 / 右键）→ **当前扳机值瞬间锁定到浮标位置**
- 继续移动 → 浮标继续走，扳机值保持上次锁定
- 再次点击 → 重新锁定到新位置
- 浮标越界（顶/底以外）点击 → 钉到 1.0 或 0.0
- 双扳机都可同时用，自动占不同的键（一边左、一边右）

### 🖱️ 方向盘 其他鼠标按键配置（新增）

方向盘编辑器底部新增「**其他鼠标按键配置**」（琥珀按钮），点开后：

- 配置左键 / 右键 / 中键 / 侧键 X1 / X2 / 滚轮↑ / 滚轮↓ 在方向盘 active 时触发什么动作（手柄键 / 手柄宏）
- 优先级低于 LT/RT 扳机；被扳机模式占用的字段会清晰标注「⚠ 不生效」
- 比如把中键映射到 `gp:LT` → 方向盘活跃时按中键 → LT 瞬时触发到 1.0（不影响进度条显示）
- 滚轮触发 `gp:LT/RT` 也能稳定生效（不会被方向盘每帧覆盖）

### 🎯 方向盘中心 hub 触发反馈

按下鼠标键触发动作时，方向盘**中心圆圈变色** + 显示触发的键名（跟普通按钮一致的视觉语言）：

- 左键 = 琥珀 / 右键 = 绿 / 中键 = 紫 / X1 = 青 / X2 = 紫 / 滚轮 = 粉
- 滚轮一次性闪 200ms 自动消失；按钮持有期间持续显示

### 🛞 方向盘改进

- **中心死区**（新增）：默认 10%（半径百分比），消除手抖误触；编辑器加「显示死区」勾选框，开后画玫红虚线 + 50% 半透明叠加
- **释放阈值**上限 300% → **500%**，更多余量
- **扳机逆向选项**：每个扳机底部加「逆向」勾选；语义跟当前 mode 联动（滚轮上减下加 / 位移上下反 / 左右键互换）
- 默认尺寸 2×2 → **3×3**（更大视觉区，更舒服）
- 方向盘**单色描边**渲染（SVG 双层叠加：填充用按钮底色不透明 + 描边用户配色），**永远不透明**，避免覆盖游戏画面时透出来
- 设置弹窗新增**「方向盘配色」**页签，支持自定义描边色
- 视觉旋转角度可选 **90° / 180° / 270° / 360° / 720°**（默认 180° — 真车感与视觉响应的平衡）
- 垂直位移单位改成**方向盘高度百分比**（默认 50%，上限 80%），跟方向盘缩放联动，不再被像素绑死
- 编辑器升级到 960×960，LT/RT 改横排卡片，三模式 tab 互斥（任一侧选了，对方对应 tab 灰掉并显示 ☑ 标记 + tooltip 说明）
- 编辑器加「恢复默认」一键按钮；布局重排：释放阈值 + 中心死区同一行各占 1/2；模式 tab 顺序「浮标点击」放最左

### ⭕ 中心轮盘重构

- **5 种环模式**自由切换：整环 / 上下半环 / 左右半环 / 四分扇区 / 八分扇区，每种 mode **各 10 套独立数据**互不影响
- **双环模式 (Dual)** 内外两个环大小一致，外八向扇面扩展到 280°，视觉更协调
- **悬停 toggle 模式**：按钮、扇区、圆环都支持开关 toggle（按一次保持触发，再按一次释放），跟按住 trigger 切换

### ⌨️ 键盘按键增强

- **悬停开关 (toggle)** 模式：不只是"按住触发"，现在每个按钮可单独设为"按一次开 / 再按一次关"，对长按操作（如蹲下、瞄准、奔跑）非常方便
- 鼠标其他按键支持：左/右/中/X1/X2/滚轮上下都能作为额外触发源
- 中心环 / 中二环单独配置 hover 模式

### ⚙️ 方案管理 v2 全面重构

- **960×960 两列布局**：左列方案列表 + 右列详情面板
- **方案详情** 4 个标签页分类展示：
  - 键盘按键：列出所有配置了动作的按钮 + 中心轮盘扇区 + 键盘宏
  - 手柄按钮：手柄键 / 摇杆 / 方向盘 + 手柄宏
  - 语音指令：开关 / 语言 / 设备 / 所有指令列表
  - 全局参数：分辨率 / 透明度 / 穿透模式 / 网格 / 模拟模式 / 外观（光标 + 方向盘色）
- **查看 vs 激活解耦**：单击方案行 = 查看详情（黄色边框），右侧「应用此方案」按钮才真正切换激活方案
- **复制方案大改**：左侧分类多选（键盘/手柄/语音/全局任意组合）+ 右侧目标选择（新建 / 已有）+ 粘贴模式（追加 / 覆盖），覆盖目标自动按宏同名冲突加 `_2/_3` 后缀
- **方向盘配色 / 光标配色 改 per-profile**，导入导出自动跟随方案

### 🔔 更新弹窗体验

- 弹窗尺寸 420×300 → **560×440**，字号 / 间距全部放大
- 修复**顶不到最上层**的问题（去掉 Qt Tool flag，show 后强制 raise + activate）
- 下载按钮**直接跳 ZIP 下载链接**，不再跳 release 网页
- 说明区放大，可显示更多内容

### 🛠️ 系统改进

- **日志诊断系统**：自动按日期切割日志，启动时打印环境信息（OS / Python / PyQt / 显示器刷新率），方便排查问题
- **系统性能调优 (P0)**：进程优先级自动提升至 HIGH_PRIORITY_CLASS，定时器跟随显示器刷新率（59Hz → 17ms 帧间隔）
- **设置弹窗多页重构**：侧边导航 + 快捷键 / 光标配色 / 方向盘配色 / 语言 / 日志 / 关于 多个独立页面
- **语音设置弹窗三列重构**：指令列表 / 编辑表单 / 候选面板分列，麦克风 + 测试按钮 + 语言下拉常驻底部
- **光标三层 SVG 配色**：底色 + 描边 + 渐变蒙版三层分离，三种穿透模式（普通/关闭/阻挡）各自独立配色 + 大小
- **运行工具栏可收起**：F4 快捷键收起为小悬浮方块，腾空屏幕空间
- **任务栏可见**：从纯透明遮罩升级为窗口，最小化时不再失踪

### ✨ 视觉打磨

- **所有弹窗自动加外发光阴影**（黑色半透明 + 24px blur），层级感更清晰
- 编辑器三态 tab ☑ 图标语义精修：自己选中白勾、对方占用同模式灰勾、其他原因禁用不画勾

### 🐛 关键 bug 修复

- **未装 ViGEmBus 驱动时启动崩溃**：vgamepad 库在 `__init__.py` 顶层执行 `VBus()` 触发 `vigem_connect`，未装驱动时抛 `VIGEM_ERROR_BUS_NOT_FOUND`（不是 `ImportError`），导致整个 app 起不来。现在改成**惰性 import** + 切手柄模式时才检测驱动，键盘模式用户完全不受影响
- **滚轮触发手柄键不响应**：`_smart_trigger` 内部期望 `'click'` action，但触摸按钮 / 中心轮盘扇区 / 圆环的滚轮信号 emit 的是 `'c'`，导致手柄键 / 宏 / 鼠标滚轮路径全部静默丢弃。已统一归一化为 `'click'`
- **方向盘 active 时按钮配 LT/RT 被立即覆盖**：方向盘每帧 `_update_wheel_triggers` 强制 `set_trigger("L"/R", final_lt/rt)`，按钮按下的 1.0 在 ~16ms 后被刷成 0。改为遍历所有 holding key 取 max，按钮和方向盘的 LT/RT 自然叠加
- **方向盘滚轮配 `gp:LT/RT` 不触发**：滚轮事件是瞬时的，press → 50ms 延迟 release 撑不过方向盘下一帧的覆盖。现在滚轮触发时也加入 holding 字典 120ms，让游戏端 60Hz polling 至少采到 6+ 帧
- **摇杆滚轮触发无视觉反馈**：小球颜色 / 键文本只对鼠标按键边沿响应，滚轮路径没调 `set_pressed_action`。现在跟方向盘对齐，滚轮触发时小球变 ACTION_COLORS 配色 + 显示键文本闪 200ms
- **手柄 click 偶尔不触发**：vgamepad 是状态机不是事件队列，press → 立即 release 在同一 flush 周期内会被驱动看到「无变化」→ 游戏收不到 button down。现在 press + flush + 延迟 50ms + release + flush，确保 press 帧被驱动看到
- **双击多次开多个编辑器**：每次双击普通按钮 / 摇杆 / 方向盘 / 中心轮盘等都创建新弹窗 → 容易开出三四个；现在每个 item 只能有一个编辑器，重复双击会激活已开的那个
- **软键盘单字符键字号**：灰色字母 / 数字键字号 15px → 24px bold，看起来填满方格不再像浮在中间
- 光标穿透中心轮盘扇面切换后按键失效的问题（信号重连）
- 编辑器关闭后部分项目缩放手柄位置错位
- 多处文案统一与本地化补全

--------

### 📥 下载

下载 `TEGGTouch_v0.3.1.zip` 解压即用，无需安装。

⚠️ **手柄模式提示**：首次切到手柄模式时会提示安装 ViGEmBus 驱动，跟着弹窗一步步操作即可。如果不用手柄模式只用键盘模式，可以忽略 ViGEmBus，蛋挞会正常启动。

### 💡 谁可以使用蛋挞？

🦾 单手用户 — 只要能操作鼠标，就能玩需要键鼠配合的游戏
🎮 手部不便的玩家 — 将复杂的多键操作简化为鼠标移动和点击
♿ 需要无障碍支持的用户 — PC 游戏全新无障碍交互方式
🏎️ 想用鼠标玩手柄游戏的玩家 — 现在赛车 / 动作 / 平台游戏也能纯鼠标
