## TEGG Touch 蛋挞 v0.3.1

TEGG Touch 是一款专为 PC 环境设计的无障碍辅助软件，永久免费、完全开源。仅用鼠标简单的移动和点击，就能替代大部分游戏操作，为单手用户和手部不便的玩家提供无障碍支持。

本次是 v0.3.0 之后的功能增强版本，方向盘扳机加了重磅新模式「浮标点击」，以及鼠标其他按键完整配置 + 中心 hub 视觉反馈 + 中心死区等改进。同时修了 v0.3.0 里几个会影响使用的 bug（更新弹窗顶不到最上层、手柄按键 click 偶尔触发不到、双击多次会开多个编辑器）。

--------

### 🏎️ 方向盘扳机第 4 种模式「浮标点击」（默认）

最适合赛车 / 弓箭蓄力 / 任何「精准给值」场景：

- 鼠标上下移动 → 方向盘 LT/RT 进度条上显示一根**横线浮标**（LT 玫红 / RT 琥珀黄）
- **浮标移动不直接修改扳机值** — 这跟「垂直位移」模式不同
- 按下你配的鼠标键（左键 / 右键）→ **当前扳机值瞬间锁定到浮标位置**
- 继续移动 → 浮标继续走，扳机值保持上次锁定
- 再次点击 → 重新锁定到新位置
- 浮标越界（顶/底以外）点击 → 钉到 1.0 或 0.0
- 双扳机都可同时用，自动占不同的键（一边左、一边右）

### 🖱️ 方向盘 其他鼠标按键配置（新增）

方向盘编辑器底部新增「**其他鼠标按键配置**」（琥珀按钮），点开后：

- 配置左键 / 右键 / 中键 / 侧键 X1 / X2 / 滚轮↑ / 滚轮↓ 在方向盘 active 时触发什么动作（手柄键 / 手柄宏）
- 优先级低于 LT/RT 扳机；被扳机模式占用的字段会清晰标注「⚠ 不生效」
- 比如把中键映射到 `gp:LT` → 方向盘活跃时按中键 → LT 瞬时触发到 1.0（不影响进度条显示）

### 🎯 方向盘中心 hub 触发反馈

按下鼠标键触发动作时，方向盘**中心圆圈变色** + 显示触发的键名（跟普通按钮一致的视觉语言）：

- 左键 = 琥珀 / 右键 = 绿 / 中键 = 紫 / X1 = 青 / X2 = 紫 / 滚轮 = 粉
- 滚轮一次性闪 200ms 自动消失；按钮持有期间持续显示

### 🛞 方向盘改进

- **中心死区**（新增）：默认 10%（半径百分比），消除手抖误触；编辑器加「显示死区」勾选框，开后画玫红虚线 + 50% 半透明叠加
- **释放阈值**上限 300% → **500%**，更多余量
- **扳机逆向选项**：每个扳机底部加「逆向」勾选；语义跟当前 mode 联动（滚轮上减下加 / 位移上下反 / 左右键互换）
- 默认尺寸 2×2 → **3×3**（更大视觉区，更舒服）
- 编辑器布局重排：释放阈值 + 中心死区同一行各占 1/2；模式 tab 顺序「浮标点击」放最左

### 🔔 更新弹窗体验

- 弹窗尺寸 420×300 → **560×440**，字号 / 间距全部放大
- 修复**顶不到最上层**的问题（去掉 Qt Tool flag，show 后强制 raise + activate）
- 下载按钮**直接跳 ZIP 下载链接**，不再跳 release 网页
- 说明区放大，可显示更多内容

### 🐛 关键 bug 修复

- **手柄 click 偶尔不触发**：vgamepad 是状态机不是事件队列，press → 立即 release 在同一 flush 周期内会被驱动看到「无变化」→ 游戏收不到 button down。现在 press + flush + 延迟 50ms + release + flush，确保 press 帧被驱动看到。影响：摇杆滚轮触发手柄键 / 方向盘其他鼠标按键触发手柄键
- **双击多次开多个编辑器**：每次双击普通按钮 / 摇杆 / 方向盘 / 中心轮盘等都创建新弹窗 → 容易开出三四个；现在每个 item 只能有一个编辑器，重复双击会激活已开的那个
- **软键盘单字符键字号**：灰色字母 / 数字键字号 15px → 24px bold，看起来填满方格不再像浮在中间

### ✨ 视觉打磨

- **所有弹窗自动加外发光阴影**（黑色半透明 + 24px blur），层级感更清晰
- 编辑器三态 tab ☑ 图标语义精修：自己选中白勾、对方占用同模式灰勾、其他原因禁用不画勾

--------

### 📥 下载

下载 `TEGGTouch_v0.3.1.zip` 解压即用，无需安装。

⚠️ **手柄模式提示**：首次切到手柄模式时会提示安装 ViGEmBus 驱动，跟着弹窗一步步操作即可。如果不用手柄模式只用键盘模式，可以忽略 ViGEmBus。

### 💡 谁可以使用蛋挞？

🦾 单手用户 — 只要能操作鼠标，就能玩需要键鼠配合的游戏
🎮 手部不便的玩家 — 将复杂的多键操作简化为鼠标移动和点击
♿ 需要无障碍支持的用户 — PC 游戏全新无障碍交互方式
🏎️ 想用鼠标玩手柄游戏的玩家 — 现在赛车 / 动作 / 平台游戏也能纯鼠标
