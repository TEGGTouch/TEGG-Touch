## TEGG Touch 蛋挞 v0.3.12 (测试版)

> 无业务改动. 唯一目的: 验证 v0.3.11 的 BOM 修复后, 自动升级是否真正端到端跑通.

### 期望表现 (v0.3.11 → v0.3.12)

1. 启动 v0.3.11 → 删 `settings\last_update_check.json` → 重启
2. 3 秒后弹「发现新版本 v0.3.12」
3. 点「立即更新」
4. UpdateDialog 显示「正在重启完成升级…」**约 2 秒**
5. 主窗口关闭 → **立刻**弹深色 splash 窗口 (520×220, TopMost):
   - 标题: 「蛋挞 正在升级到新版本」(琥珀色)
   - 状态行滚动: 等待主程序退出... → 正在解压新版本 (约 10-15 秒)... → 正在替换文件... → 正在启动新版本...
   - 底部: 「请稍候, 完成后会自动重启」
6. splash 自动消失 → v0.3.12 自动启动
7. 关于页 `v0.3.12`
8. `%TEMP%\teggtouch_update\` 应该是空的 (zip + ps1 都被删了)

### 如果还是看不到 splash / 没自动重启

第一时间发 `%TEMP%\teggtouch_updater.log` 给我, 里面应该有完整的 SetStatus 行 (中文不再乱码).

### 这一版改了什么
- `core/constants.py`: APP_VERSION `0.3.11` → `0.3.12`
