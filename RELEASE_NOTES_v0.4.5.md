## TEGG Touch 蛋挞 v0.4.5

一个**重要修复版**：修掉了一个会让配置 / 方案被保存到错误文件夹的「自更新 / 保存位置」bug，并让更新后能直接告诉你程序装在哪。

> ⚠️ **本次强烈建议直接从官网下载使用** —— 👉 https://danta.ningshen.net/
>
> 因为这版修复的正是「自更新 / 配置保存位置」相关的问题，旧版本（≤ v0.4.4）在某些启动方式下可能把配置写到非预期的目录。为了干净稳妥，**推荐这次从官网下载最新版**，而不是只依赖旧版的一键更新。

--------

### 🐛 关键修复：自更新 / 配置保存位置

- **配置不再丢失**：配置 / 方案 / 快捷键改为**原子写**（先写临时文件、整体替换），即使保存途中崩溃 / 断电 / 被强杀，也不会再出现 0 字节空文件把整份配置清空的情况。
- **保存位置固定**：profiles / 配置 / 设置 / 日志的路径全部**锚定到程序所在目录**，不再受启动方式（命令行 / 计划任务 / 被其他程序拉起）影响而跑到未知文件夹。
- **更新后告诉你装在哪**：就地更新完成后，新版首次启动会弹出「已更新到 vX + 程序位置」提示，并带一个「打开所在文件夹」按钮。更新**不会改动你原来的安装文件夹名**，从此不用再猜新版装到哪了。

### ⚙️ 设置：检查更新 / 访问官网

- 「关于」页原来的「最后更新日期」改为 **「检查更新」「访问官网」** 两个按钮。
- **检查更新**：立即联网检查（跳过冷却），有新版直接弹出更新窗口；已是最新版 / 检查失败也会有明确提示。
- **访问官网**：一键在浏览器打开官网。

--------

### 📥 下载 & 升级

- **推荐（本次）**：到 [官网](https://danta.ningshen.net/) 下载 `TEGGTouch_v0.4.5.zip`，解压到一个**新文件夹**即可使用，干净无残留。
- **想保留原有配置的老用户**：
  - 在 app 里点「立即更新」一键升级（会保留 profiles / 设置 / 日志）；**或**
  - 从官网下载后，把旧目录里的 `profiles\` 和 `settings\` 复制到新版目录即可。
- **首次安装**：下载解压即用，无需安装 Python。

### 💡 谁可以使用蛋挞？

🦾 单手用户 — 只要能操作鼠标，就能玩需要键鼠配合的游戏
🎮 手部不便的玩家 — 把复杂的多键操作简化为鼠标移动和点击
♿ 需要无障碍支持的用户 — PC 游戏全新无障碍交互方式
🏎️ 想用鼠标玩手柄游戏的玩家 — 赛车 / 动作 / 平台游戏也能纯鼠标
