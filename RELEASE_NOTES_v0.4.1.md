## TEGG Touch 蛋挞 v0.4.1

v0.4.0 之后的小版本：重做编辑器「缩放」滑块，顺便修掉缩放在非 100% 档位下的显示错位。

--------

### 🎚️ 编辑器「缩放」滑块（取代旧版「网格」滑块）

老版本工具栏的「网格」滑块（60-100px）其实是把按钮按比例缩小，**会改写按钮数据本身** — 拖一下，所有按钮坐标 / 宽高都被覆盖，拖回 100 也回不到原样。

新版改为真正的「缩放」滑块：**50% - 200%** 全范围，只影响显示，按钮数据原封不动。你可以放心拖到任何档位预览，松手 / 拖回 100% 都不会丢配置。

- 老 profile 自动迁移：`grid_size` 重置为 100（只做网格吸附），新增 `scene_scale` 字段（默认 1.0）
- 按钮数据 1:1 保留，迁移前后摆位完全一致

### 🐛 缩放显示修复

之前在非 100% 档位下会出现两个奇怪现象：

- **小于 100%**（如 60% / 75%）：整个编辑画布缩到屏幕**左上角**，网格和遮罩只占小半个屏幕，右下大片留空显示桌面
- **大于 100%**（如 130%）：中心十字、网格、按钮**整体往右下偏**，看起来像「坐标错位」

根因：视图缩放是从原点 (0,0) 缩的，没围绕屏幕中心；并且网格按场景固定边界画，不跟随可见视口。

修复后：任何档位下场景中心始终对齐屏幕中心，网格 / 遮罩自动延伸到整个屏幕，按钮坐标回到该在的位置。

--------

### 📥 下载 & 升级

- **v0.4.0 用户**：在 app 里点「立即更新」即可一键升级，你的所有 profile、设置、日志都会保留
- **首次安装**：下载 `TEGGTouch_v0.4.1.zip` 解压即用，无需安装

### 💡 谁可以使用蛋挞？

🦾 单手用户 — 只要能操作鼠标，就能玩需要键鼠配合的游戏
🎮 手部不便的玩家 — 把复杂的多键操作简化为鼠标移动和点击
♿ 需要无障碍支持的用户 — PC 游戏全新无障碍交互方式
🏎️ 想用鼠标玩手柄游戏的玩家 — 赛车 / 动作 / 平台游戏也能纯鼠标
