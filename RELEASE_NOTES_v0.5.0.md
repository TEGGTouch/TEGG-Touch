## TEGG Touch 蛋挞 v0.5.0

**新增「键盘映射」**——运行蛋挞时，可以把键盘上的任意一个键改成别的动作：换成另一个键、当手柄键用、点鼠标、跑一整套宏、启动程序或让光标回中。每个方案各存一套，互不干扰。

--------

### ⌨️ 新功能：键盘映射

以前蛋挞只能用**屏幕上的按键**、语音和摇杆来触发操作，物理键盘是原样透传的。现在物理键盘也接进了同一套动作体系：

- **换键**：`A` 改成 `D`、`W` 和 `↑` 对调，手不方便够到的键挪到顺手的位置；
- **当手柄用**：按 `W` 直接输出手柄的 `LB`，键盘玩手柄游戏；
- **点鼠标**：按 `X` 等于点一下左键，不用来回挪手；
- **跑宏**：按 `K` 触发一整套连招，和屏幕按键上的宏是同一个宏池；
- **启动程序 / 光标回中**：和屏幕按键完全一样的能力。

**三种触发模式**：

| 模式 | 行为 | 适合 |
|------|------|------|
| 按住 | 按下就按下、松开就松开 | 游戏里换键，默认用这个 |
| 单次 | 按下完整触发一次，松开不管 | 宏 / 启动程序 / 回中（这几类会自动锁到单次） |
| 锁定 | 按一次锁住不放，再按一次松开 | 自动跑步、锁血一类 |

**怎么用**：编辑界面工具栏上「软键盘」后面新增了 **⇄ 键盘映射** 按钮。点「新增映射」后直接按一下你要改掉的那个键（不用在列表里翻找），再从右侧面板点选目标动作即可。

**运行中临时要打字**：按 **F2** 一键放行整套映射，再按一次恢复。这个快捷键可以在「设置 → 快捷键」里改。

> 小提示：映射只在**运行模式**下生效，编辑界面里键盘一切照旧。另外，和蛋挞自身快捷键（F2~F12）冲突的映射会被自动忽略——否则一旦把 F12 映射掉，就再也退不出运行模式了。

### 🎮 修复：纯键盘方案也会插一个虚拟手柄

- 之前只要装了手柄驱动，**任何方案**进运行模式都会创建一个虚拟 Xbox 手柄。部分游戏检测到手柄后会自动切到手柄操作模式，导致纯键盘方案的按键操作失灵。
- 现在**只有方案里确实配了手柄触发时才会创建虚拟手柄**；从手柄方案切回键盘方案时，会把上一个方案残留的虚拟手柄一并拔掉。

### 🎨 界面细节

- 键盘映射与语音指令的列表，划过和选中时的文字配色统一，不再有突兀的黑白跳变；
- 工具栏右上角的窗口按钮前加了分隔线。

--------

### 📥 下载 & 升级

- **推荐**：到 [官网](https://danta.ningshen.net/) 下载 `TEGGTouch_v0.5.0.zip`，解压到一个新文件夹即可使用。
- **老用户一键升级**：在 app 里点「立即更新」，会保留你的 profiles / 设置 / 日志。
- **首次安装**：下载解压即用，无需安装 Python。

### 💡 谁可以使用蛋挞？

🦾 单手用户 — 只要能操作鼠标，就能玩需要键鼠配合的游戏
🎮 手部不便的玩家 — 把复杂的多键操作简化为鼠标移动和点击
♿ 需要无障碍支持的用户 — PC 游戏全新无障碍交互方式
🏎️ 想用鼠标玩手柄游戏的玩家 — 赛车 / 动作 / 平台游戏也能纯鼠标
