## TEGG Touch 蛋挞 v0.4.7

**修复了组合键（如 Ctrl+F4）点击不生效的问题**，并给悬停加了「触发间隔」，让长按能被更多游戏正确识别。

--------

### 🐛 修复：组合键点击不生效

- 以前把 **Ctrl+F4、Alt+Tab** 这类组合键设成点击时，会被逐个键拆开发送，修饰键提前松开，导致组合键**不成立、按了没反应**。现在改为「全部按下 → 逆序全部释放」，组合键**稳定触发**。

### 🕹️ 新功能：悬停「触发间隔」（长按连发）

- 悬停触发的按键，过去只在激活时按下一次并保持。对于**靠重复按键识别长按**的程序（部分游戏、连续输入、菜单连滚），会被当成"只按了一下"而非长按。
- 新增 **「触发间隔」滑块**：设为 0（默认）时保持原样，只触发一次；设成间隔后，**按住期间每隔这么多毫秒重复触发一次**，模拟键盘长按连发。最大 1 秒、步进 100ms。
- **悬停触发 / 悬停开关**两种模式，**按钮 / 轮盘扇区 / 中心轮盘**三类元件，都可以单独设置。
- 在按钮编辑弹窗的悬停区块即可调节，带说明文字。

--------

### 📥 下载 & 升级

- **推荐**：到 [官网](https://danta.ningshen.net/) 下载 `TEGGTouch_v0.4.7.zip`，解压到一个新文件夹即可使用。
- **老用户一键升级**：在 app 里点「立即更新」，会保留你的 profiles / 设置 / 日志。
- **首次安装**：下载解压即用，无需安装 Python。

### 💡 谁可以使用蛋挞？

🦾 单手用户 — 只要能操作鼠标，就能玩需要键鼠配合的游戏
🎮 手部不便的玩家 — 把复杂的多键操作简化为鼠标移动和点击
♿ 需要无障碍支持的用户 — PC 游戏全新无障碍交互方式
🏎️ 想用鼠标玩手柄游戏的玩家 — 赛车 / 动作 / 平台游戏也能纯鼠标
