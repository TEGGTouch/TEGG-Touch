## TEGG Touch 蛋挞 v0.4.2

v0.4.1 之后的功能与修复合集：修掉了一个会让**鼠标滚轮完全失效**的回归，新增 iPadOS 风格的**半透明圆球光标**，并把**轻松模式（鼠标开车）**的转向手感重做得更平稳。

> ⚠️ **强烈建议 v0.4.1 用户升级** —— v0.4.1 存在滚轮失效问题，本版已修复。

--------

### 🐛 修复：鼠标滚轮完全失效（重要）

v0.4.1 在新版 Python 运行时下，`input_engine` 与穿透管理抢同一个系统鼠标接口的参数类型，导致**滚轮映射整体不触发**（语音 / 按钮 / 手柄 / 轮盘的滚轮动作全部受影响）。本版统一了底层参数，滚轮恢复正常。

### 🖱️ 新增：半透明圆球光标（类 iPadOS 指针）

除了原来的箭头光标，现在多了一种**半透明实心圆**光标，圆心对齐真实光标顶点，观感更柔和、在游戏画面上更不挡视线。

- 颜色 / 透明度 / 大小都可调，三种穿透模式各一套（默认 白 / 蓝 / 黄 × 透明度 0.6）
- 设置「光标」页重构为**箭头组 / 圆球组**上下两栏，点哪组头部就激活哪组，激活组带 amber「✓ 激活」标记
- 「重置箭头」「重置圆球」各自独立；形状与样式 per-profile 持久化，也可在全局快捷键里改键

### 🏎️ 轻松模式（鼠标开车）转向重做

- **转向触发阈值**取代了原来从不生效的「定点死区」：现在是「增量死区」（1~5px，默认 1px），越大越抗手抖
- 新增**触发延迟 / 释放延迟**两个滑块：转向像按钮一样有缓冲——松手后 A/D 维持一会儿再回中，左右切换更顺
- 方向盘**旋转动画重做**：跟实际按键状态走，恒定角速转到满舵、保持时稳定不抖、松开后平滑回正

### 🎙️ 语音识别延迟可调

语音设置里新增「识别延迟」滑块（30~200ms），可按自己的网络 / 机器权衡灵敏度与稳定性，per-profile 保存。

### 🧷 运行界面状态持久化 + 光标开关

- 悬浮球位置、折叠状态、工具栏显隐、按键显隐现在都会**记到 profile**，下次运行原样恢复
- 运行工具栏新增「光标」开关（默认 F3，可改键）随时切换自绘光标显隐
- 悬浮球加了 hover 提示（左 / 右 / 中键 + 拖动）

### 🎨 配色「单一色源」

方向盘 / 按钮 / 摇杆现在只需调**一个基色**，描边、填充、文字会按色相自动派生成一套协调配色（默认外观与旧版完全一致）。

--------

### 📥 下载 & 升级

- **v0.4.x 用户**：在 app 里点「立即更新」即可一键升级，所有 profile、设置、日志都会保留（国内走 OSS 镜像加速）
- **首次安装**：下载 `TEGGTouch_v0.4.2.zip` 解压即用，无需安装 Python

### 💡 谁可以使用蛋挞？

🦾 单手用户 — 只要能操作鼠标，就能玩需要键鼠配合的游戏
🎮 手部不便的玩家 — 把复杂的多键操作简化为鼠标移动和点击
♿ 需要无障碍支持的用户 — PC 游戏全新无障碍交互方式
🏎️ 想用鼠标玩手柄游戏的玩家 — 赛车 / 动作 / 平台游戏也能纯鼠标
